syntax="proto3"; //版本

option java_outer_classname="StudentPOJO"; //生成的外部类名,同时也是文件名

//protobuf 使用message管理器

//会在StudentPOJO外部类生成内部类Student,它是真正发送的POJO对象
message Student{

  //Student类中有一个属性,名字为id,类型为int32(protobuf类型),1表示属性序号,不是值
  int32 id=1;

  string name=2;
}